{
  "name": "iracing-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy"
  },
  "dependencies": {
    "iracing-api": "^0.5.2"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}